{
    "name": "zong/oral-test",
    "description": "A simple PHP application on Heroku",
    "require": {
        "php": "^8.0",
        "ext-mbstring": "*",
        "ext-curl": "*",
        "vlucas/phpdotenv": "^5.4",
        "guzzlehttp/guzzle": "^7.0"
    },
    "minimum-stability": "dev",
    "prefer-stable": true,
    "scripts": {
        "post-install-cmd": [
            "php -r 'copy(\".env.example\", \".env\");'"
        ]
    }
}
